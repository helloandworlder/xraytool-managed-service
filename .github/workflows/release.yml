name: Release CI

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version, for example v0.2.0'
        required: true
        type: string
      draft:
        description: 'Create release as draft'
        required: false
        default: false
        type: boolean
      prerelease:
        description: 'Mark release as prerelease'
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref_name || github.event.inputs.version }}
  cancel-in-progress: false

env:
  GO_VERSION: '1.26.0'
  NODE_VERSION: '22'
  PNPM_VERSION: '10.28.2'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Resolve release metadata
        id: meta
        run: |
          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            version="${{ github.event.inputs.version }}"
            version="${version#refs/tags/}"
            if [[ "${version}" != v* ]]; then
              version="v${version}"
            fi
          else
            version="${GITHUB_REF_NAME}"
          fi
          echo "tag_name=${version}" >> "${GITHUB_OUTPUT}"
          echo "release_name=XrayTool ${version}" >> "${GITHUB_OUTPUT}"
          echo "commit_sha=$(git rev-parse HEAD)" >> "${GITHUB_OUTPUT}"
          echo "short_sha=$(git rev-parse --short HEAD)" >> "${GITHUB_OUTPUT}"
          echo "build_time=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "${GITHUB_OUTPUT}"
          echo "run_url=${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}" >> "${GITHUB_OUTPUT}"

      - name: Run backend tests
        run: go test ./...

      - name: Build frontend
        working-directory: frontend
        run: |
          pnpm install --frozen-lockfile
          pnpm run build

      - name: Build backend binaries locally
        run: |
          LDFLAGS="-X xraytool/internal/buildinfo.Version=${{ steps.meta.outputs.tag_name }} -X xraytool/internal/buildinfo.Commit=${{ steps.meta.outputs.commit_sha }} -X xraytool/internal/buildinfo.BuildTime=${{ steps.meta.outputs.build_time }} -X xraytool/internal/buildinfo.ProtocolVersion=1"
          go build -ldflags "$LDFLAGS" ./...

      - name: Build binaries
        run: |
          mkdir -p dist
          docker run --rm -v "$PWD":/src -w /src debian:bullseye bash -lc '
            set -euo pipefail
            apt-get update
            apt-get install -y ca-certificates curl gcc
            curl -fsSL https://go.dev/dl/go${{ env.GO_VERSION }}.linux-amd64.tar.gz -o /tmp/go.tgz
            tar -C /usr/local -xzf /tmp/go.tgz
            export PATH=/usr/local/go/bin:$PATH
            LDFLAGS="-X xraytool/internal/buildinfo.Version=${{ steps.meta.outputs.tag_name }} -X xraytool/internal/buildinfo.Commit=${{ steps.meta.outputs.commit_sha }} -X xraytool/internal/buildinfo.BuildTime=${{ steps.meta.outputs.build_time }} -X xraytool/internal/buildinfo.ProtocolVersion=1"
            CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/xraytool-linux-amd64 ./cmd/xraytool
            CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go build -ldflags "$LDFLAGS" -o dist/xraytoolctl-linux-amd64 ./cmd/xtoolctl
            CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go -C third_party/xray-fork build -buildvcs=false -o ../../dist/xray-linux-amd64 ./main
          '

      - name: Package release bundles
        run: bash scripts/package_release.sh amd64

      - name: Verify release contains the managed Xray Fork
        run: bash scripts/test_release_package.sh dist/xraytool-linux-amd64.tar.gz

      - name: Generate checksums
        run: |
          cd dist
          sha256sum \
            xraytool-linux-amd64 \
            xraytoolctl-linux-amd64 \
            xray-linux-amd64 \
            xraytool-linux-amd64.tar.gz \
            > checksums.txt

      - name: Write release evidence manifest
        run: |
          {
            printf 'release_version=%s\n' '${{ steps.meta.outputs.tag_name }}'
            printf 'git_commit_sha=%s\n' '${{ steps.meta.outputs.commit_sha }}'
            printf 'ci_run_url=%s\n' '${{ steps.meta.outputs.run_url }}'
            printf 'build_time=%s\n' '${{ steps.meta.outputs.build_time }}'
            printf 'xray_fork_tree_sha=%s\n' "$(git rev-parse HEAD:third_party/xray-fork)"
            printf 'artifact_sha256s=\n'
            cat dist/checksums.txt
          } > dist/release-manifest.txt

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: xraytool-release-${{ steps.meta.outputs.tag_name }}
          path: |
            dist/xraytool-linux-amd64
            dist/xraytoolctl-linux-amd64
            dist/xray-linux-amd64
            dist/xraytool-linux-amd64.tar.gz
            dist/checksums.txt
            dist/release-manifest.txt

      - name: Create git tag for manual release
        if: github.event_name == 'workflow_dispatch'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git rev-parse "${{ steps.meta.outputs.tag_name }}" >/dev/null 2>&1; then
            echo "Tag ${{ steps.meta.outputs.tag_name }} already exists"
          else
            git tag "${{ steps.meta.outputs.tag_name }}" "${GITHUB_SHA}"
            git push origin "${{ steps.meta.outputs.tag_name }}"
          fi

      - name: Publish GitHub release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag_name }}
          name: ${{ steps.meta.outputs.release_name }}
          draft: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.draft == 'true' || false }}
          prerelease: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.prerelease == 'true' || false }}
          generate_release_notes: true
          target_commitish: ${{ github.sha }}
          files: |
            dist/xraytool-linux-amd64
            dist/xraytoolctl-linux-amd64
            dist/xray-linux-amd64
            dist/xraytool-linux-amd64.tar.gz
            dist/checksums.txt
            dist/release-manifest.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
